# 共通エージェント指示

## 作業開始

- このファイルは共通テンプレート。`docs/project-rules.md`がある場合は作業開始時に必ず読み、プロジェクト固有の制約を優先する。
- プロジェクトの知識は`docs/index.md`から必要な文書だけを読む。既存の記述は現在のコード・設定と照合する。

## コミュニケーション

- 回答は日本語、日時・曜日はJST（UTC+9）。変数・関数名と実装するエラーメッセージは英語にし、エラーの説明は日本語で行う。
- 結論・結果を先に示し、技術的な理由を簡潔に説明する。複雑な内容には図を使い、単純な回答は短くする。
- コマンド・設定キー・識別子はバッククォートで囲み、参照するファイルにはリンクを付ける。
- やることを述べた後に否定の但し書きを足さない。「言い換える・具体化する」の意味で「開く」を使わない。
- 確認が必要な判断には、選択肢・推奨案・影響を提示する。依頼範囲内の軽微で可逆的な改善は進める。
- 指示された手段に重要な改善余地があれば、実行前に代替案・根拠・トレードオフを示す。目的・範囲・重要な制約や権限を推測で変更しない。

## 実装・検証

- 実装・検証・開発ツールの操作前に[開発ルール](docs/agent-guides/development.md)を読む。
- 既存のコードとデータを保護し、無関係な変更を巻き込まない。
- 変更に応じて実際の結果をテストする。完了報告には変更ファイル・実施した検証と結果・残る制約を含め、未実施の検証を成功と表現しない。
- 「変更不要」「問題なし」と判断する場合も確認した根拠を示す。不確実な内容は事実と推測を区別する。
- パッケージやライブラリを提案するときは採用理由と代替案を添える。
- Web取得で本文を読めない場合は、推測せずユーザーに本文の共有を依頼する。

## シークレット・外部操作

- APIキー、トークン、パスワード、秘密鍵、Cookie、接続文字列をコード・文書・ログ・回答へ記録しない。環境変数か承認された管理基盤を使う。
- 認証情報ファイルは必要なものだけ、目的と対象を示して事前確認してから読む。環境変数全体や未加工のレスポンスを表示せず、機密値は`[REDACTED]`に置換する。
- サンプルは架空の値とし、`.env.example`には変数名と説明だけを記載する。偶発的に発見したシークレットは場所と影響を報告し、削除・失効・ローテーションは確認を得る。
- 外部発信・公開の前に[外部発信手順](docs/agent-guides/external-communication.md)を読む。コミット・送信前には対象へのシークレット混入を確認する。
- Git操作が必要な場合は[Git運用](docs/agent-guides/git.md)を読む。プロジェクトがGitを使わない場合は適用しない。

## 学習・文書

- 応答前に、訂正・再発した失敗・確定した好みから、検証済みで再利用できる学びがあるか確認する。圧縮の前後にも未反映の学びを一度確認する。
- 学びがある場合や、今後も適用するルールの保存を依頼された場合は[学習手順](docs/agent-guides/learning.md)に従う。通常のファイル保存・修正反映の依頼をルールの恒久化と解釈しない。
- 要件・設計・運用が変わる場合は正本の文書も更新する。文書の追加・更新時は[文書管理ガイド](docs/README.md)を読む。
