# 重要事項

- 全て日本語で回答すること
- 日時・曜日は JST（UTC+9）で扱う。
- 技術的なわかりやすい解説を交えながら実装を進める。複雑な内容を説明する際は視覚化を活用する
- 変更不要・問題なしと結論する前に、確認した根拠を明示する。
- パッケージやライブラリを提案するときは、採用理由と代替案を簡潔に添える。
- WebFetch やブラウザ取得で本文が読めない場合は、推測せずユーザーに本文の共有を依頼する
- 実際の結果を必ずテストし、「見た目で完了」と思わない
- 既存のコードとデータを保護する
- やることを述べたあとに「〜はしません」「外部には飛びません」「（読み取りのみ）」と否定の但し書きを足さない
- 「言い換える・具体化する」の意味で「開く」を使わない。「言い換える」「具体例で書き直す」「かみ砕く」と普通に言う
- ユーザーの指示は、利用可能な情報や認識に基づくものであり、目的達成に最適な手段とは限らない。一方、Claude Codeもユーザーの事情や意向を完全には把握していない。指示された手段を、明示された目的と確認済みの意向に照らして検討し、効果・費用・安全性・実行可能性の面で重要な改善が見込める場合は、実行前に代替案とその根拠、トレードオフを簡潔に提示する。ただし、推測した「真の要望」を理由に、明示された制約や権限を勝手に変更しない。目的・範囲・重要な制約を変える場合は確認し、依頼範囲内の軽微で可逆的な改善は、不要な確認で作業を止めずに進める。

# 言語設定

このプロジェクトは日本語環境での動作を前提とする。基本的な受け答えは、全て日本語で行うこと。

- コミュニケーション: 日本語で対応
- コメント: コードコメントは日本語で記述
- エラーメッセージ: 英語で実装する。説明する場合は日本語に翻訳すること
- 変数名・関数名: 英語を使用（国際的な慣例に従う）

# プロジェクトの目的

TBD

# ドキュメント

プロジェクトの要件や仕様については`docs/README.md`を参照
`docs/`は要件・設計文書の正本であり、同時にOpen Knowledge Format（OKF）v0.2の
バンドルとして管理する。文書を探す場合は`docs/index.md`を入口とする。

# OKF

`docs/`は、エージェントと人が共有する、長期的に有効なプロジェクト知識を格納する
Open Knowledge Format（OKF）v0.2のバンドルとする。

- 作業にプロジェクト固有の知識が必要な場合は、最初に`docs/index.md`を読み、そこから関係する概念文書だけを参照する
- アーキテクチャ、ドメイン、データ、機能、運用手順などの永続的な知識が変更された場合は、実装と同じ変更内で該当する概念文書を更新する
- `docs/`の概念文書を追加・移動・削除した場合は、同じバンドルの`index.md`を更新し、重要な知識変更は`log.md`の先頭に記録する
- 概念文書はUTF-8のMarkdownとし、先頭のYAML frontmatterに空でない`type`を必ず含める
- `index.md`と`log.md`は予約ファイル名として扱い、通常の概念文書には使用しない
- タスクの進捗、一時的な調査メモ、推測、秘密情報、認証情報はOKFに記録しない
- `docs/`などに正本がある情報は重複させず、OKFから正本を案内する

# 学習ループ

- **応答を締める前に毎回ひと呼吸おいて自己チェックする。** 次のトリガーが出たターンは、指示を待たず先回りで恒久化する（該当なし・一回性の些末事なら何もしない／宣言も不要）。
  - **訂正・不満・既出指摘**：「違う」「そうじゃない」「ではなく」「前も言った」「毎回」「また同じ」「なんで勝手に」「余計」など
  - **作業の罠・失敗**：想定外のエラー、ハマった箇所、音声入力の誤変換パターン
  - **確定した好み・成功パターン**、明示の保存依頼
- **訂正・不満・再発報告を受けたターンは、原因の説明や改善案の提示だけで終えない。** その場で安全に実施できる範囲なら、①直接原因の修正 ②同種入力でも壊れない仕組み側の再発防止 ③該当ルール／スキルへの学習 ④各ツールへの同期 ⑤テストと commit・push まで、追加指示を待たずに完了する。質問形の「なぜ？」「いつもこうなる」も、再発する不具合の報告なら修正依頼を兼ねるものとして扱う。外部送信・公開範囲変更・削除・課金など新しい権限が必要な操作と、結果を大きく変える選択だけは従来どおり確認する。
- 反映は再利用価値があるものだけ。ノイズルールの蓄積は設定の劣化そのものなので、無理に絞り出さない。
- **コンテキスト圧縮が近い／行われた節目では、圧縮前の会話で得た学びを反映し損ねていないか一度だけ確認する。** 圧縮で詳細が失われると恒久化の機会も消えるため。該当が無ければ何もしない。

## ウェイクワード「学習しといて」と保存先ルーティング

「学習しといて」「覚えておいて」「保存して」「反映して」はウェイクワード。これらを言われたら、**適切なスコープを自分で判断して反映する**。

1. **スコープを判断**：全プロジェクト共通か、特定リポジトリ固有か（迷ったら推測せず確認する）。
2. **反映先を選ぶ**：ルール・ガイドラインか、手順・ノウハウ（Skills）か、プロジェクト固有の事実かで振り分ける。
3. **特定ツールでしか読めない場所に入れない**。複数のエージェントツールを併用している場合、片方のダイナミックメモリはもう片方から読めない。原則として**両ツールが読める場所**へ書く。
4. **伝播まで1セット**：ユーザースコープなら設定同期スキル、プロジェクトスコープなら git push まで実施する。
5. **報告**：どこに何を書いたかを簡潔に伝える。

| 覚えたい内容 | 保存先（両ツール可読） | 伝播手段 |
|---|---|---|
| 全プロジェクト共通の好み・ルール | この共通 `AGENTS.md` | 各ツールの設定同期スキル |
| プロジェクト固有の運用・事実 | 対象リポジトリの `AGENTS.md` | git push |
| 再利用可能な手順・ノウハウ | 共有スキル（各ツールのスキルディレクトリへ同内容で） | 設定同期 |
| 片方のツール固有の挙動・最適化のみ | そのツールのダイナミックメモリ | 不要 |
